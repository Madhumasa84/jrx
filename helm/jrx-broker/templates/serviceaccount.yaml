{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jrx-broker.serviceAccountName" . }}
  labels:
    {{- include "jrx-broker.labels" . | nindent 4 }}
{{- end }}
